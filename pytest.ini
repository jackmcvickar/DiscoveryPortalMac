[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: marks tests as unit-level (fast, isolated)
    integration: marks tests as integration-level (full pipeline)
# end of script